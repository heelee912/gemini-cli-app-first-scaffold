# Fictional Recruiter Profile Test

This example is intentionally fictional. It is a public-safe brief for testing
the scaffold without using private career data.

Run:

```powershell
python ..\..\scripts\run_gemini_design_once.py ..\..\out\fictional-profile --name "Fictional Recruiter Profile" --brief-file brief.md --force
```
